spring:
  application:
    name: eshop-zuul-gateway
  cloud:
    config:
      uri: http://127.0.0.1:8051
      label: master
      profile: online2
      discovery:
        enabled: true
        service-id: eshop-config-server
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8030/eureka/